= form_for @user_ do |f|
  - if @user_.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_.errors.count, "error")} prohibited this user_ from being saved:"
      %ul
        - @user_.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
